/** CSS Modules: compiled by lightningcss at build time into a hashed class-name map (see tsdown.config.ts). */
declare module '*.module.css' {
  const classes: Record<string, string>
  export default classes
}
