# 本包贡献的配置层：把皮肤插进 Loader 树。
# profile 列出这个包时自动应用（见 apps/cli/reference：dsh.profile.bundles）。
#
# 没有这一层的话，`dsh plugin add` 只会把包装成普通依赖，插件永远不会被加载
# —— dsh 会当场警告 “declares no dsh.bundle … not a profile layer”。
- insert:
    - id: niulai
      name: dsh-niulai
